<div class="container">

  <form id="search-movies" class="form-inline p-3">
    <input id="keyword" type="text" class="form-control ml-auto mr-2" placeholder="Find movies">
    <input type="submit" class="btn btn-primary mr-auto ml-2" value="Search">
  </form>

  <div id="results" class="row d-flex justify-content-between align-items-stretch"></div>

  <script>
    const movieResults = document.querySelector("#results");

    const searchMovies = (query) => {
    fetch(`https://api.themoviedb.org/3/search/movie?api_key=2048d887fa53babd4687e73aa2f418f4&language=en-US&page=1&query=${query}`)
      .then(response => response.json())
      .then((data) => {
        const movieArray = data.results
        movieArray.forEach((result) => {
          const movie =`
          <div class="col-auto mb-3">
            <div class="card">
              <img class="card-img" src="https://image.tmdb.org/t/p/w200${result.poster_path}" alt="">
              <h4 class ="card-title">${result.original_title}</h4>
              <a href="#"><i class="fas fa-plus-circle fa-lg add-button" style="float:right;"></i></a>
            </div>
          </div>`;
          movieResults.insertAdjacentHTML("beforeend", movie);
      });
    });
  }

    const searchForm = document.querySelector("#search-movies")

    searchForm.addEventListener("submit", (event) => {
      event.preventDefault();
      results.innerHTML = "";
      const keyword = document.querySelector("#keyword").value;
      console.log(keyword)
      searchMovies(keyword)
    });
  </script>

  <!--

    <div class="films row d-flex justify-content-between align-items-stretch">

      <div class="col-auto mb-3">
        <div class="card" >
          <img class="card-img" src= " Movie.find(bookmark.movie_id).poster_url"alt="nope" style=" width:305; height: 460;">
          <h4 class = "card-title"><link_to Movie.find(bookmark.movie_id).title, movie_path(bookmark.movie_id) %></h4>
          <p>Movie.find(bookmark.movie_id).overview[0..90]%>...</p>
          <p>Movie.find(bookmark.movie_id).rating %> ✭</p>
        </div>
      </div>
    </div>
   -->
</div>
