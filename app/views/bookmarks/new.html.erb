<div class="container">
  <h2>Add a bookmark to <%= @list.name %></h2>

  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment %>
    <%= f.submit "Add bookmark" %>
  <% end %>

  <%= link_to "Add a new film", results_path %>
</div>

