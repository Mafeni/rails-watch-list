<div class="container">

  <div class="header">
    <h1>✪ Welcome to the Watchlist ✪</h1>
    <a href="<%= new_list_path %>"><i class="fas fa-plus-circle fa-lg add-button"></i></a>
  </div>


  <div class="lists row d-flex justify-content-between align-items-center">
    <% @lists.each do |list| %>
    <div class="col-auto mb-3">
      <div class="card">
        <%= cl_image_tag list.picture.key, class: "card-img", height: 300, width: 400, crop: :fill %>
        <h4 class = "card-title"><%= link_to list.name, list_path(list) %></h4>
        <p><%= link_to '🗑', list_path(list), method: :delete %></p>
      </div>
     </div>
    <% end %>
  </div>


</div>

